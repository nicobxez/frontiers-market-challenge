export const CONSOLE_PKG_NAME = '@frontiersmarket';

export const authContextProvider = {
  missingProviderErrorMessage: `${CONSOLE_PKG_NAME}: userAuth context must be used wrapped by AuthContextProvider.`,
};
